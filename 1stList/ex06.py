#Make a programm that asks for a circle ray and print it's area.

ray = float(input('Type the value of a circle ray: '))

area = (ray * ray) * 3.14

print(f"A Circle Ray with the {ray} value, has {area} as it's area.")
