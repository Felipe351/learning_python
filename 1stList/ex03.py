#Make a programm that asks for two numbers and them prints the sum of both.

first = int(input('Type any number: '))
second = int(input('Type one more number: '))

sum = first + second

print(f'{first} + {second} = {sum}')
